import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ReportsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Stage 1 · Proof"
      title="Reports"
      description="The certification ledger — who passed what, at which version, and when."
      owner="Builder B is building the certifications ledger + CSV export."
    />
  );
}
