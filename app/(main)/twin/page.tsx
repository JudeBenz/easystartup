import { RoutePlaceholder } from "@/components/route-placeholder";

export default function TwinPage() {
  return (
    <RoutePlaceholder
      eyebrow="Stage 3 · Teach the robot"
      title="Spatial twin"
      description="Scan the space, pin every job to where it happens, and pair it with a robot-readable spec."
      owner="Builder B is building the Stage 3 twin teaser."
    />
  );
}
