import { RoutePlaceholder } from "@/components/route-placeholder";

export default function WorkspaceSettingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Settings"
      title="Workspace"
      description="Organization details and demo controls."
      owner="Builder B owns workspace settings (optional / light)."
    />
  );
}
