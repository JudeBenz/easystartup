import { RoutePlaceholder } from "@/components/route-placeholder";

export default function AutopilotPage() {
  return (
    <RoutePlaceholder
      eyebrow="Stage 2 · Autopilot"
      title="Autopilot"
      description="Is the business open yet? The daily opening sequence across every role."
      owner="Builder B is building the autopilot dashboard."
    />
  );
}
