import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ProcedureDetailPage() {
  return (
    <RoutePlaceholder
      eyebrow="Stage 1 · Procedurize"
      title="Procedure detail"
      description="Steps, versions, assignments, and Start training."
      owner="Builder A is building procedure detail + versioning."
    />
  );
}
